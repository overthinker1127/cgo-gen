# 02 C++ Class

C++ class/free-function header with constructor, destructor, namespace, and `std::string`.

```bash
cargo run --bin cgo-gen -- check --config examples/02-cpp-class/config.yaml
cargo run --bin cgo-gen -- generate --config examples/02-cpp-class/config.yaml --dump-ir
```

- `input/counter.hpp`: input header
- `input/counter.cpp`: matching implementation for reference
- `generated/`: committed generator output
